{
    "type": "object",
    "required": [ "displayName", "name", "email", "avatarUrl", "service", "tokens" ],
    "properties": {
        "displayName": {
            "type": "string",
            "description": "must be a string and is required"
        },
        "name": {
            "type": "string",
            "description": "must be an string is required"
        },
        "email": {
            "type": "string",
            "description": "must be an email string is required"
        },
        "avatarUrl": {
            "type": "string",
            "description": "must be an string is required"
        },
        "bio": {
            "type": "string",
            "description": "must be an string is not required"
        },
        "service": {
            "type": "string",
            "description": "must be an string is not required"
        },
        "tokens": {
            "type": "object",
            "description": "must be an object is not required"
        },
        "streamInfo": {
            "type": "object",
            "description": "must be an object is not required"
        },
        "streamInfoLastUpdateDate": {
            "type": "object",
            "description": "must be an date-time is not required"
        },
        "live": {
            "type": "boolean",
            "description": "must be an boolean is not required"
        }
    }
}
